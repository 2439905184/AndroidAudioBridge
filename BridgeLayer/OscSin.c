// 不依赖底层的数学正弦波实现
#include <stdlib.h>

float buffer_size[4];
//y=sin(wx+b)
int index = 0;
while(TRUE)
{
    index += 1;
    buffer_size[i];
    //1111 0000 1111 0000;
    if(index % 4 == 0)
    {
        index = 0;
        next = 0;
    }
    else
    {
        next = 1;
    }
    buffer_size[index] = next;
}